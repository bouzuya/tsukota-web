# 043

- application crate における `account` `category` `export` `owner` `transaction` という単位での mod を削除して、内容をフラットにしてください
- たとえば `src/request/account.rs` は `src/request/create_account_request.rs` と `src/request/update_account_request.rs` と `src/request/add_owner_request.rs` に分割してください
