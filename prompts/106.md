# 106

- backend/ の api crate に struct SessionTokenClaims を追加します
- JWT の claims として使用します
- aud, exp, iss, sub をフィールドに持たせてください
- aud は `"tsukota-web"` で固定です
- exp は JWT における標準的な使用をします。ただし、通常より長く 12 h に設定します
- iss は `https://bouzuya.net/lab/tsukota` で固定です
- sub にはユーザー ID を設定します

