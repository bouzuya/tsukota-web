# 081

- EventStoreRepository の Firestore 実装を修正します
- is_new_account で判定している箇所を event document の作成前に持っていき、 match で記述してください
- user_updates のコメントに、 owner ごとに 1 件ずつの UserUpdateAction で十分になることを明記してください
- 内部では ApplicationError の使用を避け、 ここでのエラーを網羅したエラー型 E を定義して、最後に `impl From<E> for ApplicationError` で変換してください
- match のパターンに `_` を使用するのをやめ、すべての variant を明記してください
