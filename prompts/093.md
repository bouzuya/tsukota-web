# 093

- infra crate に DeviceRepository の実装を追加してください
- Firestore の `aggregates/device/event_streams/{device_id}` に `DeviceEventStreamDocumentData` の形式でドキュメントを作成するようにしてください
- `DeviceEventStreamDocumentData` は `id: String` と `updated_at: String` をフィールドに持ってください
- Firestore の `aggregates/device/event_streams/{device_id}/events/{device_event_id}` に `DeviceEvent` の形式でドキュメントを作成するようにしてください
- `DeviceEvent` は domain crate に定義されています
- Firestore の `devices/{device_id}` に `DeviceDocumentData` の形式でドキュメントを作成するようにしてください
- `DeviceDocumentData` は id: String, encryptedSecret: String (Rust では snake_case, serde の rename_all で、 JSON では camelCase にしてください), uid: String をフィールドに持ってください
