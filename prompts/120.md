# Prompt

lib.rs のルールをドキュメントに追加する。

lib.rs には `mod xxx;` と `pub use self::xxx::...;` のみを記述し、実装コードは別モジュールに配置する。

# Plan: lib.rs のルールをドキュメントに追加

## 概要

`lib.rs` には `mod xxx;` と `pub use self::xxx::...;` のみを記述し、実装コードは別モジュールに配置するルールを CODING_GUIDELINES.md に追加する。

## 変更対象ファイル

- `docs/CODING_GUIDELINES.md`

## 変更内容

「モジュール構成」セクション（39行目〜99行目付近）に新しいサブセクション「lib.rs の構成ルール」を追加する。

### 追加する内容

```markdown
#### lib.rs の構成ルール

**ルール**: `lib.rs` には `mod` 宣言と `pub use` のみを記述し、実装コードは配置しない

\`\`\`rust
// ✅ 推奨: lib.rs
mod date_time;

pub use self::date_time::DateTime;
pub use self::date_time::ParseDateTimeError;
\`\`\`

\`\`\`rust
// ❌ 避ける: lib.rs に実装コードを直接配置
pub struct DateTime(chrono::DateTime<Utc>);

impl DateTime {
    pub fn now() -> Self {
        // ...
    }
}
\`\`\`

**理由**:
- モジュール構造の一貫性を保つ
- 各モジュールの責務を明確にする
- テストコードを含む実装を独立したファイルに分離できる
```

### 追加位置

「モジュールファイルの配置」セクション（39行目）の後、「Rust コーディングスタイル」セクション（100行目）の前に挿入する。

## 検証手順

変更後、ドキュメントの内容を確認し、Markdown の構文が正しいことを確認する。
