services:
  workspace:
    command: sleep infinity
    environment:
      - FIRESTORE_EMULATOR_HOST=firebase:8080
    image: debian:trixie
    volumes:
      - .:/workspaces/tsukota-web:cached
      - ~/.claude:/home/vscode/.claude
      - ~/.copilot:/home/vscode/.copilot

  firebase:
    build: ./firebase
    ports:
      - "4000:4000"
      - "8080:8080"
    volumes:
      - ./firebase:/firebase
